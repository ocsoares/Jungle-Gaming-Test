{
  "name": "@repo/user-exceptions",
  "private": true,
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "clean": "rimraf dist tsconfig.tsbuildinfo",
    "build": "pnpm run clean && tsc -p tsconfig.json",
    "build:force": "pnpm run clean && tsc -p tsconfig.json --force"
  },
  "devDependencies": {
    "rimraf": "^6.1.2"
  },
  "dependencies": {
    "@nestjs/common": "^11.1.9"
  }
}